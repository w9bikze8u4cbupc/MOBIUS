// AI utilities placeholder module
export function explainChunkWithAI(chunk) {
  return Promise.resolve("Mock AI explanation of chunk");
}

export function extractComponentsWithAI(text) {
  return Promise.resolve([
    { name: "Game Board", quantity: 1 },
    { name: "Player Pieces", quantity: 4 },
    { name: "Cards", quantity: 52 }
  ]);
}