# Browser Configuration Example
# Copy this file to .env and customize as needed

# Default browser for development
# Options:
#   google-chrome    - Google Chrome (recommended, avoids localhost blocking)
#   firefox          - Mozilla Firefox
#   msedge           - Microsoft Edge (Windows/macOS)
#   safari           - Safari (macOS only)
#   chromium-browser - Chromium (Linux)
#   none             - Don't auto-open browser
BROWSER=google-chrome

# Usage Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env to change BROWSER if needed
# 3. Run: npm start
#
# Platform-specific browser commands:
# - Windows: google-chrome, msedge, firefox
# - macOS: google-chrome, safari, firefox, msedge
# - Linux: google-chrome, chromium-browser, firefox
#
# Troubleshooting:
# - If browser doesn't open, try BROWSER=none and open manually
# - Brave users may experience localhost blocking - use Chrome instead
# - For corporate networks, set BROWSER=none if proxy issues occur