# ---------------------------------------------------------------------------
# Core Mobius configuration template
# ---------------------------------------------------------------------------

# API key rotation
MOBIUS_API_KEY_STORE=var/security/api_keys.json
MOBIUS_API_KEY_ROTATION_INTERVAL=30d
MOBIUS_API_KEY_GRACE_PERIOD=2h

# Audit logging
MOBIUS_AUDIT_LOG=logs/audit.log

# Rate limiting - format METHOD:/path=COUNT/WINDOW_SECONDS
MOBIUS_RATE_LIMITS=GET:/health=60/60,POST:/save-project=30/60,POST:/api/explain-chunk=90/60

# Security headers - comma separated Header:Value entries
MOBIUS_SECURITY_HEADERS=Strict-Transport-Security:max-age=63072000; includeSubDomains,X-Content-Type-Options:nosniff,X-Frame-Options:DENY,Referrer-Policy:no-referrer

# CORS policies - comma separated allowlist
MOBIUS_CORS_ALLOWED_ORIGINS=https://mobius.example.com,https://studio.mobius.example
